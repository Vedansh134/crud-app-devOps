kind: Service
apiVersion: v1
metadata:
  name: backend-service
  namespace: crudapp-backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000
    protocol: TCP
    nodePort: 31000

