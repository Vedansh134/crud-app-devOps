global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'student-app'
    static_configs:
      - targets: ['student-app:5000']  # This matches container name
    metrics_path: /metrics